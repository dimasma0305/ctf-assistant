version: '3'
services:
  ctf-assistant:
    build: .
    restart: on-failure
    volumes:
      - /home/ubuntu/.ssh/:~/.ssh/
    networks:
      default:
      internal:
networks:
  default:
  internal:
    internal: true
